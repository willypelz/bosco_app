"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
const policy = require("snyk-policy");
const display_policy_1 = require("../../lib/display-policy");
const errors_1 = require("../../lib/errors");
function displayPolicy(path) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const loadedPolicy = yield policy.load(path || process.cwd());
            return yield display_policy_1.display(loadedPolicy);
        }
        catch (error) {
            if (error.code === 'ENOENT') {
                error = new errors_1.PolicyNotFoundError();
            }
            else {
                error = new errors_1.FailedToLoadPolicyError();
                error.innerError = error;
            }
            throw error;
        }
    });
}
module.exports = displayPolicy;
//# sourceMappingURL=policy.js.map